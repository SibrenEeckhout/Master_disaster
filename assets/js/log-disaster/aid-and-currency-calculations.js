"use strict";

// add the aid goal to the list
function determineAid(disaster, aid)
{
    let Aid = 5 * counter * 100
    counter = 0
    aid.push(Aid)
    determineCurrency(disaster, aid)
}

//add the currency goal to the list
function determineCurrency(disaster, list)
{
    let currency = level_info * (country_info.length - 1) * 200
    list.push(currency, aid_info)
    list.push(0)
    if (disastername_info !== 'Minor hostage situation')
    {
        submitDisaster(list)
    }
}
// Add additional functions below